#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
alias grep='grep --color=auto'
PS1='$? [\u@\h $(pwd)]\$ '
export EDITOR=nvim
export PATH=$PATH:$HOME/go/bin
source ~/.config/bash/eww_completions.sh
source ~/.config/bash/fzf.sh
